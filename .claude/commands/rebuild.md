# Rebuild - Full Autonomous Pipeline

Create a `rebuild/[date]` branch, then run full autonomous pipeline: dep map → read → reconstruct intent → audit → fix → verify → report.

Your working branch is never touched. Merge when satisfied.

Load and execute agents/meta/rebuild-specialist.md immediately.
